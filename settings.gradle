pluginManagement {
    // Locally defined plugin to be included as part of build
    // Do this, plus apply local gradle plugin from in this module in top level build.gradle
    // includeBuild("build-logic")
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}

println 'Executing OnHand settings.gradle - initialization phase.'
rootProject.name = "OnHand"
include ':app'
include ':core'
include ':core:domain'
include ':core:data'
include ':core:ui'
include ':core:model'
include ':core:network'
